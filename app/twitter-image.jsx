// Twitter/X share card — same branded 1200×630 image as the Open Graph card.
// Re-exported so the visual stays in one place (app/opengraph-image.jsx).
export { default, alt, size, contentType } from "./opengraph-image";
